# Девелоперская архитектура — <название продукта>

> Слой 3 из 3 · «КАК запускать и как ИИ проверяет себя» · Дата: <ГГГГ-ММ-ДД>
> Основа: docs/spec/01-functional-requirements.md, docs/spec/02-tech-architecture.md
<!-- В существующем проекте добавь строку: > Режим: существующий проект («как есть → как хотим») -->

## Запуск одной командой

- **Команда:** `<одна команда>` → <что поднимается: приложение + локальная база с тестовыми данными>
- **Для человека:** открыть <локальный адрес>, войти как <тестовый пользователь>

## Тестовые данные и сброс

- **Seed:** <что в наборе: тестовый пользователь, пара записей — по одной строке>
- **Сброс:** <как вернуть окружение в чистое известное состояние одной командой>

## Петля проверки (запустил → прогнал → доказал → починил)

| Сценарий (из фич слоя 1) | Как ИИ прогоняет | Чем доказывает результат |
|--------------------------|------------------|--------------------------|
| <например: регистрация → загрузка → генерация → результат в кабинете> | <в реальном браузере, по шагам> | <скриншот экрана + строка в базе> |

> «Ошибок нет» — не доказательство. Доказательство — наблюдаемый результат: скриншот, запись в базе, ответ сервера.

## Инструменты для ИИ

| Инструмент | Зачем |
|-----------|-------|
| <браузерная автоматизация, например Playwright MCP> | ИИ сам открывает экраны, кликает, делает скриншоты |
| <MCP к базе / логам> | ИИ сам проверяет, что данные легли |
| <мини-скиллы: «прогони тесты», «сбрось окружение»> | типовая проверка вызывается по имени |

## Локальные заменители (при автоматических проверках)

| Компонент (из слоя 2) | Локальный заменитель | Зачем |
|-----------------------|----------------------|-------|
| <например: ИИ-модель> | <фиксированные ответы> | быстро, бесплатно, предсказуемо |
| <база / хранилище / почта> | <локальный аналог> | не трогаем реальные данные |

## Открытые вопросы

- <по одной строке; если пусто, раздел убери>
