<!-- Каркас DESIGN.md по спецификации Google design.md
     (https://github.com/google-labs-code/design.md).
     Файл пишется в КОРЕНЬ проекта под именем DESIGN.md.
     Точную схему YAML-токенов сверь со спецификацией: npx @google/design.md spec.
     Готовый файл проверь: npx @google/design.md lint. -->

---
name: <название продукта>
description: <одна строка: что за продукт и каким он должен ощущаться>
colors:
  background: "#FFFFFF"
  foreground: "#111111"
  primary: "#2563EB"
  secondary: "#F3F4F6"
typography:
  heading: "<шрифт заголовков>"
  body: "<шрифт текста>"
rounded: "<например 8px>"
spacing: "<базовый шаг, например 4px>"
components:
  button: "<одна строка: как выглядит основная кнопка>"
  card: "<одна строка>"
  input: "<одна строка>"
---

## Overview

<2–3 строки: настроение продукта (строгий/дружелюбный, плотный/воздушный) и откуда оно —
источник: тема «имя» (ссылка) / сайты-образцы (URL — что взяли) / каталог design.md.
UI kit проекта: <shadcn/ui, MUI…>. Дата: <ГГГГ-ММ-ДД>.>

## Colors

<По строке на роль: фон, текст, акцент (кнопки, ссылки), второстепенный, успех/ошибка.
У каждого цвета: значение · токен кита (например `--primary` у shadcn) · откуда
(тема / сайт-образец / «уже было в проекте»).>

## Typography

<По строке на роль: заголовки и текст — шрифт, начертания, кириллица ✅, лицензия
(бесплатно/OFL…), прямая ссылка. Шкала размеров: например 14 / 16 / 20 / 24 / 32.
Шрифт заменён → одна строка: «в теме/на образце X, заменён на Y: нет кириллицы / платный».>

## Layout

<Плотность и ритм: базовый отступ, ширина контента, сколько воздуха. Откуда.>

## Elevation & Depth

<Тени и слои: есть/нет, какие. Одной-двумя строками.>

## Shapes

<Радиусы скруглений: значение · токен (`--radius`) · откуда.>

## Components

<По строке на ключевой компонент: кнопка основная, поле формы, карточка — как выглядит,
в терминах токенов кита.>

## Do's and Don'ts

<Анти-слоп-запреты и правила, по строке: «не больше двух шрифтов», «без градиентов»,
«цвета только токенами темы, хардкод запрещён»…>

<!-- Только если в шаге 6 были конфликты с прежней темой — добавь в конец Overview
     строки: токен · было · стало · решение человека (заменить / оставить / смешать).
     Открытые вопросы — тоже в Overview, по строке; нет — не пиши. -->
